<UserControl x:Class="TOSAi.TeacherApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="620"
             d:DesignWidth="880">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <ItemsControl ItemsSource="{Binding Metrics}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Margin" Value="0,0,14,18" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="{Binding Label}" Style="{StaticResource MutedText}" />
                                <TextBlock Text="{Binding Value}"
                                           Margin="0,10,0,5"
                                           FontSize="30"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource TextMainBrush}" />
                                <TextBlock Text="{Binding Hint}"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="学科表现" Style="{StaticResource SectionTitle}" />
                        <DataGrid ItemsSource="{Binding Subjects}" Height="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="学科" Binding="{Binding SubjectName}" Width="80" />
                                <DataGridTextColumn Header="班级均分" Binding="{Binding ClassAverage}" Width="90" />
                                <DataGridTextColumn Header="年级均分" Binding="{Binding GradeAverage}" Width="90" />
                                <DataGridTextColumn Header="优势人数" Binding="{Binding AdvantageCount}" Width="90" />
                                <DataGridTextColumn Header="风险人数" Binding="{Binding RiskCount}" Width="90" />
                                <DataGridTextColumn Header="观察" Binding="{Binding Comment}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="本周关注" Style="{StaticResource SectionTitle}" />
                        <TextBlock Text="1. 物理风险学生集中在实验探究题。" Margin="0,6,0,0" TextWrapping="Wrap" />
                        <TextBlock Text="2. 英语低分段学生词汇基础不足。" Margin="0,12,0,0" TextWrapping="Wrap" />
                        <TextBlock Text="3. 数学优势学生可进入拔高题训练。" Margin="0,12,0,0" TextWrapping="Wrap" />
                        <Border Background="{StaticResource SoftSurfaceBrush}" CornerRadius="8" Padding="14" Margin="0,22,0,0">
                            <TextBlock Text="后续可把这里替换成 AI 自动生成的教学提醒、学生跟进清单或备课建议。"
                                       Foreground="{StaticResource TextMainBrush}"
                                       TextWrapping="Wrap" />
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>

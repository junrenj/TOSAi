<UserControl x:Class="TOSAi.TeacherApp.Views.StudentTrendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="620"
             d:DesignWidth="880">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,18">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right"
                        Content="刷新云端数据"
                        Style="{StaticResource PrimaryButton}"
                        Click="RefreshButton_Click" />

                <StackPanel Orientation="Horizontal">
                    <StackPanel Width="240">
                        <TextBlock Text="学生" FontWeight="SemiBold" />
                        <ComboBox x:Name="StudentComboBox"
                                  DisplayMemberPath="DisplayName"
                                  SelectedValuePath="StudentId"
                                  Margin="0,8,18,0"
                                  MinHeight="34"
                                  SelectionChanged="StudentComboBox_SelectionChanged" />
                    </StackPanel>
                    <StackPanel Width="200">
                        <TextBlock Text="学科" FontWeight="SemiBold" />
                        <ComboBox x:Name="SubjectComboBox"
                                  Margin="0,8,18,0"
                                  MinHeight="34"
                                  SelectionChanged="SubjectComboBox_SelectionChanged" />
                    </StackPanel>
                    <TextBlock x:Name="StatusText"
                               VerticalAlignment="Bottom"
                               Margin="0,0,0,8"
                               Foreground="{StaticResource TextMutedBrush}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="学生摘要" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="考试次数" Style="{StaticResource MutedText}" />
                    <TextBlock x:Name="ExamCountText" Text="-" FontSize="28" FontWeight="Bold" Margin="0,6,0,18" />
                    <TextBlock Text="平均得分率" Style="{StaticResource MutedText}" />
                    <TextBlock x:Name="AverageRateText" Text="-" FontSize="28" FontWeight="Bold" Margin="0,6,0,18" />
                    <TextBlock Text="优势学科" Style="{StaticResource MutedText}" />
                    <TextBlock x:Name="BestSubjectText" Text="-" FontSize="18" FontWeight="SemiBold" Margin="0,6,0,18" />
                    <TextBlock Text="薄弱学科" Style="{StaticResource MutedText}" />
                    <TextBlock x:Name="WeakSubjectText" Text="-" FontSize="18" FontWeight="SemiBold" Margin="0,6,0,0" />

                    <Border Background="{StaticResource SoftSurfaceBrush}" CornerRadius="8" Padding="14" Margin="0,24,0,0">
                        <TextBlock Text="趋势页会读取“成绩录入”页面保存到云端的数据。没有数据时，请先导入 CSV 并保存云端。"
                                   TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}">
                <TabControl>
                    <TabItem Header="趋势列表">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                            <ItemsControl x:Name="TrendItemsControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="210" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="96" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding ExamName}" FontWeight="SemiBold" />
                                                    <TextBlock Text="{Binding ExamDate}" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Margin="16,0">
                                                    <TextBlock Text="{Binding SubjectName}" FontWeight="SemiBold" />
                                                    <ProgressBar Value="{Binding ScoreRate}" Maximum="100" Height="12" Margin="0,8,0,0" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding ScoreRate, StringFormat={}{0}%}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" />
                                                    <TextBlock Text="{Binding ScoreText}" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Right" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="明细表">
                        <DataGrid x:Name="TrendDataGrid" Margin="0,12,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="考试名称" Binding="{Binding ExamName}" Width="160" />
                                <DataGridTextColumn Header="考试日期" Binding="{Binding ExamDate}" Width="100" />
                                <DataGridTextColumn Header="班级" Binding="{Binding ClassName}" Width="110" />
                                <DataGridTextColumn Header="学科" Binding="{Binding SubjectName}" Width="80" />
                                <DataGridTextColumn Header="分数" Binding="{Binding Score}" Width="80" />
                                <DataGridTextColumn Header="满分" Binding="{Binding FullScore}" Width="80" />
                                <DataGridTextColumn Header="得分率%" Binding="{Binding ScoreRate}" Width="90" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>

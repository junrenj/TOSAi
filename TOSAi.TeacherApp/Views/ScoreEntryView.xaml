<UserControl x:Class="TOSAi.TeacherApp.Views.ScoreEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="620"
             d:DesignWidth="880">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,18">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="导入 CSV" Style="{StaticResource PrimaryButton}" Click="ImportScoresButton_Click" />
                    <Button Content="导出模板" Margin="10,0,0,0" Style="{StaticResource SecondaryButton}" Click="ExportTemplateButton_Click" />
                    <Button Content="保存本地" Margin="10,0,0,0" Style="{StaticResource SecondaryButton}" Click="SaveButton_Click" />
                    <Button Content="读取本地" Margin="10,0,0,0" Style="{StaticResource SecondaryButton}" Click="LoadLocalButton_Click" />
                    <Button Content="清空本地" Margin="10,0,0,0" Style="{StaticResource SecondaryButton}" Click="ClearLocalButton_Click" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="成绩录入与校验" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="使用 CSV 模板录入成绩，可直接用 Excel 打开编辑。当前支持导出模板、导入校验和明细预览。"
                               Style="{StaticResource MutedText}" />
                    <TextBlock x:Name="ImportStatusText"
                               Margin="0,8,0,0"
                               Foreground="{StaticResource SuccessBrush}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <TabControl>
            <TabItem Header="按学生汇总">
                <Border Style="{StaticResource Card}" Padding="0" Margin="0,12,0,0">
                    <DataGrid ItemsSource="{Binding Scores}" Margin="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="姓名" Binding="{Binding StudentName}" Width="100" />
                            <DataGridTextColumn Header="班级" Binding="{Binding ClassName}" Width="110" />
                            <DataGridTextColumn Header="语文" Binding="{Binding Chinese}" Width="80" />
                            <DataGridTextColumn Header="数学" Binding="{Binding Math}" Width="80" />
                            <DataGridTextColumn Header="英语" Binding="{Binding English}" Width="80" />
                            <DataGridTextColumn Header="物理" Binding="{Binding Physics}" Width="80" />
                            <DataGridTextColumn Header="化学" Binding="{Binding Chemistry}" Width="80" />
                            <DataGridTextColumn Header="总分" Binding="{Binding Total}" Width="90" IsReadOnly="True" />
                            <DataGridTextColumn Header="平均分" Binding="{Binding Average}" Width="90" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <TabItem Header="导入明细">
                <Border Style="{StaticResource Card}" Padding="0" Margin="0,12,0,0">
                    <DataGrid x:Name="ImportRowsDataGrid" Margin="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="考试名称" Binding="{Binding ExamName}" Width="150" />
                            <DataGridTextColumn Header="考试日期" Binding="{Binding ExamDate}" Width="100" />
                            <DataGridTextColumn Header="年级" Binding="{Binding GradeName}" Width="80" />
                            <DataGridTextColumn Header="班级" Binding="{Binding ClassName}" Width="110" />
                            <DataGridTextColumn Header="学号" Binding="{Binding StudentId}" Width="110" />
                            <DataGridTextColumn Header="姓名" Binding="{Binding StudentName}" Width="90" />
                            <DataGridTextColumn Header="学科" Binding="{Binding SubjectName}" Width="80" />
                            <DataGridTextColumn Header="分数" Binding="{Binding Score}" Width="80" />
                            <DataGridTextColumn Header="满分" Binding="{Binding FullScore}" Width="80" />
                            <DataGridTextColumn Header="得分率%" Binding="{Binding ScoreRate}" Width="90" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
    </DockPanel>
</UserControl>


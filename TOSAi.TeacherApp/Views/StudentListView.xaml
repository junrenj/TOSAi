<UserControl x:Class="TOSAi.TeacherApp.Views.StudentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="620"
             d:DesignWidth="880">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,18">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="新增学生" Style="{StaticResource SecondaryButton}" Click="PlaceholderButton_Click" />
                    <Button Content="批量导入" Margin="10,0,0,0" Style="{StaticResource PrimaryButton}" Click="PlaceholderButton_Click" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="学生基础档案" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="这里先放示例学生数据，后续可接入数据库、Excel 或学校教务系统。"
                               Style="{StaticResource MutedText}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Students}" Margin="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="学号" Binding="{Binding StudentId}" Width="120" />
                    <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="100" />
                    <DataGridTextColumn Header="班级" Binding="{Binding ClassName}" Width="110" />
                    <DataGridTextColumn Header="总分" Binding="{Binding TotalScore}" Width="80" />
                    <DataGridTextColumn Header="年级排名" Binding="{Binding GradeRank}" Width="90" />
                    <DataGridTextColumn Header="优势学科" Binding="{Binding StrongSubject}" Width="100" />
                    <DataGridTextColumn Header="薄弱学科" Binding="{Binding WeakSubject}" Width="100" />
                    <DataGridTextColumn Header="跟进建议" Binding="{Binding Advice}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
